[phases.setup]
cmds = [
    "pip install -r requirements.txt",
    "mkdir -p logs data/uploads data/reports models/cache"
]

[phases.install]
cmds = ["echo 'Installation complete'"]

[start]
cmd = "python text_auth_app.py"